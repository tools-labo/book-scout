name: Manga Items (DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This workflow is temporarily disabled while lane2 is being rebuilt."
